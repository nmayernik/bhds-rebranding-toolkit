SHOWCASE_PASSWORD=bhds

# Optional: token required by /api/comments/export. Set the same value in the
# Vercel production env and in the GitHub Actions repo secret ARCHIVE_TOKEN so
# the archive workflow can snapshot comments into .comments-archive/.
ARCHIVE_TOKEN=
